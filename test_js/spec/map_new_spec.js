describe("Validate form", function() {
	it("should alert the user when input is NaN", function() {
		expect()
	})
}

beforeEach(function() {
	validateForm = jasmine.createSpy("validateForm");
	validateForm("alerted as usual")
});